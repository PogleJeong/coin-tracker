/*
    REACT-QUERY 사용

    타입스크립트에서 react query 를 불러오기 위해서 
    npm i react-query
    npm i @tanstack/react-query

    index.tsx 에서 처음설정 (provider 패턴);
*/

function Chart() {
    return(
        <h1>
            Chart!
        </h1>
    )
}
export default Chart;
